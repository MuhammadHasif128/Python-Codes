s = input('Enter a String: ')

for i in range(len(s)):
    print("Character", s[i], "is at position", i)

# s. shows the type of functions that work on a string
# len autocalculate ur length of words, the index
